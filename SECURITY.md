# Security Policy

本文说明 annona 的安全边界、漏洞上报方式，以及**已实现**与**规划中**的区别。

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| `0.x`（开发中，尚未发布） | 无安全支持承诺——P0 阶段代码仅含骨架 |
| `v1.0` 及之后的最新 minor | 计划支持（修复高危与中危） |

发布策略见 [`docs/annona-开发计划.md`](docs/annona-开发计划.md)。**首个正式版本发布前，本仓库不应被用于处理真实敏感数据。**

## 报告漏洞

- **优先使用 GitHub 私有安全报告**：仓库首页 → Security → **Report a vulnerability**。该功能需要在仓库设置中开启（已登记为开发计划的仓库设置事项）。开启前，请把高危细节通过加密渠道发给维护者，**不要直接开公开 issue**。
- **非敏感问题**（功能缺陷、文档错误）走普通 [Issues](https://github.com/Ember452/annona/issues)。
- 报告中请尽量包含：复现步骤、影响版本、是否为托管/自部署哪种模式、可行的缓解方式。
- 响应时间：本人为单人维护者，承诺 **7 天内回复**；修复排期视严重程度而定。请勿在修复发布前公开披露细节。

## 已实现的安全设计（设计层面，非"已上线代码"）

以下条款定义了这个项目的安全模型，实现进度以开发计划为准：

1. **不内置任何模型 API Key**。代码、配置、Docker 镜像、seed 数据、测试夹具中均不得出现可用 Key；`.env.example` 只含占位符，真实 `.env` 被 `.gitignore` 忽略。详见 [`docs/specs/2026-09-25-model-api-key-adr.md`](docs/specs/2026-09-25-model-api-key-adr.md)。
2. **Key 加密存储**：AES/GCM + 每记录 `nonce`；加密主密钥（KEK）来自环境变量 `ANNONA_SECRET_KEY`，与密文分离存储；生产环境缺失 KEK 时应用**拒绝启动**（fail-fast），不使用静默 fallback。
3. **明文 Key 不下发前端**：接口只返回掩码（`sk-****abcd`）；日志与异常栈中的 Key 必须脱敏；解密只发生在服务端调用模型的瞬间且不进入缓存。
4. **口令与会话**：口令用 scrypt 哈希（每用户随机 salt）；活跃会话只存 Redis（HttpOnly + SameSite Cookie），数据库侧仅保留审计投影；一次性凭据（改密/验证）只存哈希。详见 [`docs/specs/2026-09-25-identity-credential-storage-adr.md`](docs/specs/2026-09-25-identity-credential-storage-adr.md)。
5. **数据隔离**：所有业务表强制携带 `user_id`，查询按用户维度过滤。
6. **注入防护**：SQL 全参数化；模型输出与用户文档中的 Markdown 走白名单净化渲染；Prompt 侧做注入清洗。
7. **限流与熔断**：`@RateLimit`（Global/IP/User 维度）+ 每日 token 配额与超额熔断，防止自部署实例被脚本刷穿。

## 明确不做的事（因此不构成"漏洞"）

- **不提供多人协作与租户隔离之外的企业级权限模型**（无 RBAC / SSO / 审计合规导出）。
- **不做任何服务端人脸、表情、姿态分析**，因此不存在该类生物特征数据处理。
- **不提供群聊、@提及、自由文本广播等用户生成内容通道**，故无此类内容审核与 XSS 面。
- **语音面试的原始音频默认不持久化**，仅保留转写文本；如未来改变该行为，会先写入 ADR 并在本文更新。
- 自部署实例的**网络暴露与 TLS 终结由部署者负责**（`deploy/nginx/` 提供参考配置）。把实例直接暴露公网而不加反代/认证不属于本项目缺陷。

## 依赖与自动化

- CI 阶段包含密钥扫描（gitleaks，pre-commit + workflow 双道）。Dependabot 依赖升级告警在 P0-P1a 骨架阶段未启用（无真实部署与 CVE 暴露面，自动 PR 噪声大于价值）；P1b-10 首次接 BYOK 真实 Key 前会写一条 ADR 重新引入，届时本文件会同步更新。
- 供应链风险接受度：本项目优先减少中间件与依赖数量（单库 PG + Redis + S3），以降低被攻击面；新增依赖须先通过 [`AGENTS.md`](AGENTS.md) §0.6 的引入评估。
